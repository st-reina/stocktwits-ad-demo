# Stocktwits Ad Demo Builder
Internal sales-enablement mockup of the Stocktwits ticker page. Sellers set a client
ticker (auto-pulls name/logo/watchers from Stocktwits) and upload creatives into every
ad slot to show clients exactly where their campaign runs. Self-contained single HTML file.
